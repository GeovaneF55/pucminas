package algorithms;

/**
 * Class that contains some images operations like
 * histogram and probability density function (PDF).
 * @author Pertence.
 */
public class Histogram {

    /**
     * Calculates the histogram of a image.
     * @param A Input image.
     * @return Returns the matrix representing the image.
     */
    public int[] hist(int A [][]){
        int H[] = null;
        return H;
    }

    /**
     * Calculates the probability density function (PDF).
     * @param A Source image.
     * @return Returns a vector containing the probabilities.
     */
    public int[] pdf(int A [][]){
        int D[] = null;
        return D;
    }
}
