package algorithms;

/**
 * Class with method to compare two images.
 * @author Pertence
 */
public class Compare {

    /**
     * Method to calculate the mean square (MSE) error between images.
     * @param A First image.
     * @param B Second image.
     * @return Returns the MSE.
     */
    public int mse(int [][] A, int [][] B){
        return 0;
    }

    /**
     * Method to calculate the mean square error (MSE) between two histograms.
     * @param A First histogram.
     * @param B Second histogram.
     * @return Returns the MSEH.
     */
    public int mseh(int[] A, int [] B){
        return 0;
    }

    /**
     * Method to calculate the relationship between the peek signal noise
     * between two images (PSNR).
     * @param A First image.
     * @param B Second image.
     * @return Returns the PSNR.
     */
    public int psnr(int [][] A, int [][] B){
        return 0;
    }
}
