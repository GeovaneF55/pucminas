package algorithms;

/**
 * Class with methods to do logarithm transformations
 * in an image.
 * @author Pertence.
 */
public class Tranformation {

    /**
     * Logarithm transformation.
     * @param A Input image matrix.
     * @return Returns an image with the filter applied.
     */
    public int [][] logIntensity(int A [][]){
        return A;
    }
}
